#include <stdbool.h>

void yopt_print(const char *format, ...)
	__attribute__ ((__format__ (__printf__, 1, 2)));
void *yopt_realloc(void *ptr, unsigned long size);
extern int yopt_tail;

#define	YOPTS_PARSE_OK		0
#define	YOPTS_PARSE_ERR		-1
#define YOPTS_PARSE_SPECIAL	1

struct yopts {
	${STRUCTURE}
};

extern struct yopts ${PROJ}yopts;
extern int ${PROJ}yopts_parse(int argc, char **argv);
extern void ${PROJ}yopts_usage(char *prog);

${CHOICES}
